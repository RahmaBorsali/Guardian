<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- SMS -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Boot & Service -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Sensors (declared for clarity) -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />

    <!-- ContentProvider custom permission -->
    <permission
        android:name="com.guardian.track.READ_EMERGENCY_CONTACTS"
        android:protectionLevel="signature|privileged"
        android:label="@string/perm_label_read_contacts"
        android:description="@string/perm_desc_read_contacts" />

    <uses-permission android:name="com.guardian.track.READ_EMERGENCY_CONTACTS" />

    <application
        android:name=".GuardianTrackApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuardianTrack"
        tools:targetApi="34">

        <!-- Single Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GuardianTrack">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground Surveillance Service -->
        <service
            android:name=".service.SurveillanceService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Battery Low BroadcastReceiver (static/manifest-registered) -->
        <receiver
            android:name=".receiver.BatteryReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BATTERY_LOW" />
            </intent-filter>
        </receiver>

        <!-- Boot Completed BroadcastReceiver (static/manifest-registered) -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Emergency Contacts ContentProvider -->
        <provider
            android:name=".provider.EmergencyContactsProvider"
            android:authorities="com.guardian.track.provider"
            android:exported="true"
            android:readPermission="com.guardian.track.READ_EMERGENCY_CONTACTS"
            android:writePermission="com.guardian.track.READ_EMERGENCY_CONTACTS" />

        <!-- WorkManager Initialization (disable default) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>