package com.guardian.track.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * GuardianTrack — Unique Cybersecurity Color Palette
 * Deep navy/charcoal base with neon-electric accent gradient
 * Deliberately distinctive from standard Material templates.
 */

// ── Primary Background Tones ──
val NightAbyss = Color(0xFF0A0E1A)         // Deepest background
val DarkNavy = Color(0xFF0F1629)           // Card backgrounds
val MidnightSteel = Color(0xFF161D35)      // Elevated surfaces
val SlateArmor = Color(0xFF1E2747)         // Surface variant

// ── Accent Gradient Colors ──
val CyberCyan = Color(0xFF00E5FF)          // Primary accent
val ElectricBlue = Color(0xFF2979FF)       // Secondary accent
val NeonViolet = Color(0xFF7C4DFF)         // Tertiary accent
val PlasmaGreen = Color(0xFF00E676)        // Success / synced
val SolarAmber = Color(0xFFFFAB00)         // Warning
val CriticalRed = Color(0xFFFF1744)        // Error / critical alert

// ── Text Colors ──
val PureWhite = Color(0xFFFFFFFF)
val GhostWhite = Color(0xFFE8EAF6)        // Primary text on dark
val SilverMist = Color(0xFF9EA7C4)         // Secondary text on dark
val DimGray = Color(0xFF5C6484)            // Disabled / tertiary text

// ── Glass / Overlay ──
val GlassWhite = Color(0x1AFFFFFF)         // 10% white for glassmorphism
val GlassBorder = Color(0x33FFFFFF)        // 20% white for borders
val GlassHighlight = Color(0x0DFFFFFF)     // 5% white subtle highlight

// ── Light Theme Colors ──
val LightBackground = Color(0xFFF6F8FA)    // Très doux, gris-bleuté
val LightSurface = Color(0xFFFFFFFF)       // Blanc pur pour les cartes
val LightSurfaceVariant = Color(0xFFEDF1F6) // Variantes douces
val LightOnBackground = Color(0xFF2D3748)  // Gris sombre (moins agressif que le noir pur)
val LightOnSurface = Color(0xFF4A5568)     // Gris moyen pour les textes
val LightSecondaryText = Color(0xFF718096) // Gris clair pour textes secondaires
